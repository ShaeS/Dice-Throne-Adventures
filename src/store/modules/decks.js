import GameTiles from '../../decks/tiles'
import GameMonsters from '../../decks/monsters'

const initialState = {
    monsters: GameMonsters,
    tiles: GameTiles
}

export default {
	state: initialState
}
